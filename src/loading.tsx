import { Typography } from "@material-ui/core";
import * as React from "react";

export const Loading: React.FC = () => {
  return <Typography color="inherit">Loading...</Typography>;
};

export default Loading;
